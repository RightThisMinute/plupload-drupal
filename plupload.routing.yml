plupload.upload:
  path: '/plupload-handle-uploads'
  defaults:
    _content: '\Drupal\plupload\UploadController::handleUploads'
    _title: 'Handles uploads'
  requirements:
    _permission: 'access_content'
    _csrf_token: 'TRUE'
